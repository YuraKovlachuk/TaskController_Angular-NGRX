// import {AppState} from "../app.state";
// import {createSelector} from "@ngrx/store";
// import {TaskState} from "./task.reducers";
//
//
// export const taskSelect = (state: AppState) => state.tasks
// export const tasksSelector = createSelector(
//   taskSelect,
//   (state: TaskState) => state.tasks
// )
// export const tasksErrorSelector = createSelector(
//   taskSelect,
//   (state: TaskState) => state.error
// )
// // export const taskSelectById = createSelector(
// //     taskSelect,
// //     (state: TaskState) => {
// //       return state.tasks.find(task => task._id === state.selectedId)
// //     }
// //     )
//
